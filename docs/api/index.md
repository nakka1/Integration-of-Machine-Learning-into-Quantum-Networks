# API reference

Every public class and function in `quantum_twin` is documented here,
generated directly from the source's docstrings (Google style) via
[mkdocstrings](https://mkdocstrings.github.io/). If a page here and the
source disagree, the source is authoritative -- these pages are rebuilt
from it, not maintained separately.

Use the navigation on the left to jump to a specific module, or the
search bar (top right) to find a specific class or function by name.

## Quick links

- Core pipeline: [`channel_simulator`](channel_simulator.md), [`models`](models.md),
  [`quantum_node`](quantum_node.md), [`orchestrator`](orchestrator.md)
- Experiments: [`pareto_sweep`](pareto_sweep.md), [`model_comparison`](model_comparison.md),
  [`ablation`](ablation.md), [`walk_forward`](walk_forward.md)
- Evaluation: [`metrics`](metrics.md), [`statistics_tests`](statistics_tests.md), [`sensitivity`](sensitivity.md)
- Infrastructure: [`reproducibility`](reproducibility.md), [`plotting`](plotting.md),
  [`latex_export`](latex_export.md), [`experiment_tracking`](experiment_tracking.md),
  [`mlops`](mlops.md), [`config`](config.md), [`cli`](cli.md)
